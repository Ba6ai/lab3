# Мартелов Елисей Группа ИТС1 Лабораторная №3



## Задание 1



### Задача 1



### Текст задачи



#### Получить список из сумм цифр натуральных чисел, содержащихся в исходном списке используя Seq.map



### Алгоритм решения



#### В функции input:
##### 1) Создаётся генератор ленивой последовательности (Seq.initInfinite)(генератору не нужен входящий элемент - _), она не считает всё сразу а ждёт пока попросят следующий элемент, и только тогда берёт элемент из консоли(Console.ReadLine).
##### 2) Условие для выхода (Seq.takeWhile - функция проверяет каждую введённую строку и пока условие x <> "ex" не будет введенно бесконечный генератор не остановиться.
##### 3) Фильтр с трансформацией (Seq.choose) - если пользователь ввёл не число, то TryParse выдаст false и вернёт в None в Seq.choose. После чего выбросит этот элемент из последовательности и заставит Seq.initInfinite считать следующую строку с консоли.
##### 4) Seq.map - берёт функцию sumCount и применяет её к каждому элементу последовательности, создавая на выходе новую последовательность с результатами.

#### Функция поиска суммы sumCount - берёт остаток от деления на 10 и складывает его с результатом. целую часть делит на 10 и потом уже от неё берёт остаток от деления на 10 и так пока в целой части не будет 0.

#### Ленивая последовательность Seq работает в таком порядке: 1)Seq.iter требует элемент у 2)Seq.map(при этом map должен посчитать сумму), он в свою очередь требует элемент у 3)Seq.choose которая запрашивает у 4)Seq.takeWhile строку (takeWhile проверяет условие выхода), которая в свою очередь запрашивает у 5) Seq.initInfinite строку, а она уже вызывает Console.ReadLine и пользователь вводит число. И это число летит обратно вверх по цепочке. Только после это Seq.iter просит следующий элемент.

### Тестирование



![Image](Image/z1.1.jpg)




## Задание 2



### Задача 1



### Текст задачи



#### Найти сумму тех элементов списка, которые начинаются на заданную цифру испольузя Seq.fold



### Алгоритм решения



#### Запрашивается число у пользователя, хранит его в значении target и если надо завершить ввод ожидает ввода "ex". В функции input:
##### 1) Создаётся генератор ленивой последовательности (Seq.initInfinite)(генератору не нужен входящий элемент - _), она не считает всё сразу а ждёт пока попросят следующий элемент, и только тогда берёт элемент из консоли(Console.ReadLine). 
##### 2) Условие для выхода (Seq.takeWhile - функция проверяет каждую введённую строку и пока условие x <> "ex" не будет введено бесконечный генератор не остановиться.
##### 3) Фильтр с трансформацией (Seq.choose) - если пользователь ввёл не число, то TryParse выдаст false и вернёт в None в Seq.choose. После чего выбросит этот элемент из последовательности и заставит Seq.initInfinite считать следующую строку с консоли.

#### В функции totalSum происходит обработка функции Seq.fold - это жадная операция, которая не может вернуть ленивую инструкцию, она обязана пройтись по всем элементам чтобы посчитать число:
##### Она проверяет если текущее число = числу с которого должны начинаться числа. То берёт текущее значение аккумулятора acc и прибавляет к нему новое число х, на следующем шагу создаёт новое значение аккумулятора(acc + x) и уже к нему добавляет подходящий элемент. Если не подходит текущее значение acc передаётся дальше.
#### В функции search происходит поиск первого символа числа. int(string s.[0])

#### Последовательность работы: 1) Запрашивает цифру у пользователя, 2) Seq.fold требует данные, 3) запрос отправится в Seq.initInfinite Console.ReadLine, 4) пользователь вводит число и возвращается в Seq.fold который складывает подходящее число со своим аккумулятором, 5) снова запрашивает число у Seq.initInfinite, 6) Завершится когда пользователь введёт "ex".


### Тестирование



![Image](Image/z2.1.jpg)



## Задание 3



### Задача 1



### Текст задачи



#### Вывести список файлов, имя которых начинается с заданного символа, в указанном каталоге.




### Алгоритм решения



#### Определяется путь до папки с файлами, вызывается функция symbol для считывания символа с консоли.
#### Работа внутри блока Directory.Exists(path):
##### 1) Объявляется конвейер files. Первые три метода Enumerates, map, filter - ленивые.
##### 2) Seq.map преобразует целый путь в короткое имя.
##### 3) Seq.filter проверяет чтобы строка не была пустой и ищет подходящие слова
##### 4) Дойдя до Seq.toList компьютеру нужен весь список целиком и он обращается к Seq.filter запрашивая у него первый подходящий элемент.
##### 5) filter отправляет запрос map чтобы он вернул элемент для проверки.
##### 6) map просит у Directory.EnumerateFiles путь к файлу и возвращает map`у целый путь
##### 7) map трансформирует целое имя в короткое (было - C:\\user\\folder\\File.txt), (стало - File.txt).
##### Происходит пока Directory.EnumerateFiles не скажет что файлы закончились. Тогда toList закрывает список и передаёт его в files
##### 8) printfn выводит готовый список на экран.


### Тестирование



![Image](Image/z3.1.jpg)